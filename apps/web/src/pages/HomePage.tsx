export type HomePageProps = {
  /**
   * Compatibility prop retained for earlier route tests.
   * Phase 2.5 Home page no longer uses this value.
   */
  apiBaseForDisplay?: string;
};

export { HomePage } from "@/pages/buyer/HomePage";
